#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long a, b;
    cin >> a >> b;

    cout << 3 << '\n';
    cout << b << " " << b << " " << (3*a - 2*b) << '\n';

    return 0;
}